init python:

    def bleep_koume(event, **kwargs):
            if event == "show_done":
                renpy.music.play("audio/bleep_koume.ogg", channel="sound", loop=True)
            elif event == "slow_done" or event == "end":
                renpy.music.stop(channel="sound", fadeout=1)

    def bleep_mc(event, **kwargs):
            if event == "show_done":
                renpy.music.play("audio/bleep_mc.ogg", channel="sound", loop=True)
            elif event == "slow_done" or event == "end":
                renpy.music.stop(channel="sound", fadeout=1)

    def bleep_asahi(event, **kwargs):
            if event == "show_done":
                renpy.music.play("audio/bleep_asahi.ogg", channel="sound", loop=True)
            elif event == "slow_done" or event == "end":
                renpy.music.stop(channel="sound", fadeout=1)

    def bleep_tsukishi(event, **kwargs):
            if event == "show_done":
                renpy.music.play("audio/bleep_tsukishi.ogg", channel="sound", loop=True)
            elif event == "slow_done" or event == "end":
                renpy.music.stop(channel="sound", fadeout=1)

    def bleep_shimon(event, **kwargs):
            if event == "show_done":
                renpy.music.play("audio/bleep_shimon.ogg", channel="sound", loop=True)
            elif event == "slow_done" or event == "end":
                renpy.music.stop(channel="sound", fadeout=1)

    def bleep_lisa(event, **kwargs):
            if event == "show_done":
                renpy.music.play("audio/bleep_lisa.ogg", channel="sound", loop=True)
            elif event == "slow_done" or event == "end":
                renpy.music.stop(channel="sound", fadeout=1)

    def bleep_reishou(event, **kwargs):
            if event == "show_done":
                renpy.music.play("audio/bleep_reishou.ogg", channel="sound", loop=True)
            elif event == "slow_done" or event == "end":
                renpy.music.stop(channel="sound", fadeout=1)

    def bleep_ari(event, **kwargs):
            if event == "show_done":
                renpy.music.play("audio/bleep_ari.ogg", channel="sound", loop=True)
            elif event == "slow_done" or event == "end":
                renpy.music.stop(channel="sound", fadeout=1)

    def bleep_triss(event, **kwargs):
            if event == "show_done":
                renpy.music.play("audio/bleep_triss.ogg", channel="sound", loop=True)
            elif event == "slow_done" or event == "end":
                renpy.music.stop(channel="sound", fadeout=1)

    def bleep_hio(event, **kwargs):
            if event == "show_done":
                renpy.music.play("audio/bleep_hio.ogg", channel="sound", loop=True)
            elif event == "slow_done" or event == "end":
                renpy.music.stop(channel="sound", fadeout=1)

    def bleep_toeko(event, **kwargs):
            if event == "show_done":
                renpy.music.play("audio/bleep_toeko.ogg", channel="sound", loop=True)
            elif event == "slow_done" or event == "end":
                renpy.music.stop(channel="sound", fadeout=1)

    def bleep_aro(event, **kwargs):
            if event == "show_done":
                renpy.music.play("audio/bleep_aro.ogg", channel="sound", loop=True)
            elif event == "slow_done" or event == "end":
                renpy.music.stop(channel="sound", fadeout=1)

    def bleep_maruu(event, **kwargs):
            if event == "show_done":
                renpy.music.play("audio/bleep_maruu.ogg", channel="sound", loop=True)
            elif event == "slow_done" or event == "end":
                renpy.music.stop(channel="sound", fadeout=1)

    def bleep_shigure(event, **kwargs):
            if event == "show_done":
                renpy.music.play("audio/bleep_shigure.ogg", channel="sound", loop=True)
            elif event == "slow_done" or event == "end":
                renpy.music.stop(channel="sound", fadeout=1)

    def bleep_mono(event, **kwargs):
            if event == "show_done":
                renpy.music.play("audio/bleep_mono.ogg", channel="sound", loop=True)
            elif event == "slow_done" or event == "end":
                renpy.music.stop(channel="sound", fadeout=1)
                
    def bleep_sat(event, **kwargs):
            if event == "show_done":
                renpy.music.play("audio/bleep_sat.ogg", channel="sound", loop=True)
            elif event == "slow_done" or event == "end":
                renpy.music.stop(channel="sound", fadeout=1)